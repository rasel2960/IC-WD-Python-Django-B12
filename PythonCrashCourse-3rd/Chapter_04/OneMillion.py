# 4-4. One Million: Make a list of the numbers from one to million, and then use a for loop to print the numbers. (If the output is taking too long, sto it by pressing CTRL-C by closing the output windows.)
numbers = list(range(1, 1000001))
for number in numbers:
    print(number)
